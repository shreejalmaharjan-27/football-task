const Loading = () => {
    return (
        <div className="w-full h-full flex items-center justify-center border border-gray-300 rounded-2xl p-6">
            <p className="text-lg font-bold">Loading...</p>
        </div>
    )
};

export default Loading;